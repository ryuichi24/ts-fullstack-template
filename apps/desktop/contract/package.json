{
  "name": "@ts-fullstack-template/desktop-contract",
  "version": "1.0.0",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "dev": "yarn build && yarn tsc:watch",
    "build": "tsc",
    "tsc:watch": "tsc --watch",
    "postinstall": "yarn build"
  },
  "devDependencies": {
    "@ts-fullstack-template/tsconfig": "*"
  }
}
