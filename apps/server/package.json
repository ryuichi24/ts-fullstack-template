{
  "name": "@ts-fullstack-template/server",
  "version": "1.0.0",
  "main": "src/index.ts",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "yarn tsc:watch",
    "dev:web": "yarn build && concurrently \"yarn tsc:watch\" \"yarn node:watch\"",
    "build": "tsc && tsx scripts/build.ts",
    "node:watch": "node --watch dist/index.js",
    "tsc:watch": "cross-env ESBUILD_WATCH=true tsx scripts/build.ts"
  },
  "dependencies": {
    "@ts-fullstack-template/process-event-emitter": "*",
    "@ts-fullstack-template/web-socket": "*",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@ts-fullstack-template/esbuild-config": "*",
    "@ts-fullstack-template/tsconfig": "*",
    "@types/ws": "^8.5.10",
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  }
}
